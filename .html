<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>BariBaba — From Amar to Tomar (Demo)</title>
  <meta name="description" content="BariBaba demo — search flats, plots, PGs. Demo includes search and simulated login.">

  <!-- Google font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
  /* ---------- CSS (all-in-one) ---------- */
  :root{
    --purple-1:#4f3e9b; --purple-2:#7b6ef3; --accent:#2ec58b; --muted:#6b7280; --container:1200px;
  }
  *{box-sizing:border-box}
  body{font-family:"Poppins",system-ui,-apple-system,Segoe UI,Roboto,Arial; margin:0; color:#111; background:#fff}
  .container{max-width:var(--container);margin:0 auto;padding:0 18px}

  /* NAV */
  .navbar{background:#241b3d;color:#fff; position:sticky; top:0; z-index:60}
  .navbar-inner{display:flex;align-items:center;justify-content:space-between;padding:12px 0}
  .brand{display:flex;align-items:center;gap:10px}
  .brand .logo{height:40px;width:40px;border-radius:6px;background:#fff;padding:6px;display:inline-block}
  .brand .brand-text{font-weight:600;font-size:1.05rem;color:#fff}
  .main-nav{display:flex;gap:18px}
  .nav-link{color:#dfe6ff;text-decoration:none;font-weight:500}
  .nav-actions{display:flex;gap:10px;align-items:center}
  .btn-outline{color:#fff;background:transparent;border:1px solid rgba(255,255,255,0.06);padding:8px 12px;border-radius:8px;text-decoration:none}
  .btn-post{background:#ff6b6b;color:#fff;padding:8px 14px;border-radius:8px;text-decoration:none;font-weight:600;border:none;cursor:pointer}

  /* HERO */
  .hero{position:relative;overflow:hidden;padding:72px 0 80px}
  .hero-image{position:absolute;right:0;top:0;height:100%;width:50%;object-fit:cover;opacity:0.98;pointer-events:none}
  .hero-overlay{position:absolute;inset:0;background:linear-gradient(135deg,var(--purple-1),var(--purple-2));mix-blend-mode:multiply;opacity:0.95}
  .hero-content{position:relative;padding:20px 0;color:#fff;text-align:center}
  .hero-title{font-size:2.4rem;margin:0 0 8px;font-weight:700}
  .hero-sub{margin:0 0 22px;color:rgba(255,255,255,0.92)}

  /* SEARCH CARD */
  .search-card{display:inline-block;background:rgba(255,255,255,0.12);border-radius:14px;padding:18px 18px 14px;box-shadow:0 8px 24px rgba(15,23,42,0.25);width:86%;max-width:980px;text-align:left}
  .tabs{display:flex;gap:14px;list-style:none;padding:0;margin:0 0 12px;align-items:center;overflow:auto}
  .tab{padding:6px 10px;border-radius:8px;color:rgba(255,255,255,0.85);font-weight:600;cursor:pointer;white-space:nowrap}
  .tab.active{background:rgba(255,255,255,0.12);box-shadow:inset 0 -2px 0 rgba(255,255,255,0.06)}
  .search-controls{display:flex;gap:12px;align-items:center}
  .search-controls select, .search-controls input{padding:12px 14px;border-radius:12px;border:none;outline:none;font-size:0.95rem}
  .search-controls select{min-width:140px;background:#fff;color:#111}
  .search-controls input{flex:1;background:#fff}
  .btn-search{background:var(--accent);color:#fff;border:none;padding:11px 22px;border-radius:12px;font-weight:700;cursor:pointer}

  /* popular chips */
  .popular{margin-top:16px;display:flex;flex-direction:column;align-items:center;gap:10px}
  .popular-label{color:rgba(255,255,255,0.95);font-weight:600}
  .chips{display:flex;gap:10px;overflow:auto;padding:6px 14px}
  .chip{background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.06);color:#fff;padding:10px 14px;border-radius:999px;cursor:pointer;white-space:nowrap}

  /* results */
  .results-section{padding:28px 18px 40px}
  #resultsTitle{margin:0 0 12px;font-weight:700}
  .results-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(270px,1fr));gap:18px}
  .card{background:#fff;border-radius:10px;overflow:hidden;box-shadow:0 6px 18px rgba(0,0,0,0.06);transition:transform .18s;cursor:default}
  .card:hover{transform:translateY(-6px)}
  .card img{width:100%;height:160px;object-fit:cover}
  .card-body{padding:12px}
  .card .price{color:var(--accent);font-weight:700}

  /* features */
  .features{padding:48px 0}
  .feature-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:18px;margin-top:18px}
  .feature-card{background:#fff;padding:18px;border-radius:12px;box-shadow:0 8px 24px rgba(15,23,42,0.06);display:flex;gap:12px;align-items:flex-start}
  .icon-wrap{width:48px;height:48px;background:linear-gradient(180deg,#f3efff,#efe7ff);display:flex;align-items:center;justify-content:center;border-radius:12px}
  .feature-card h3{margin:0 0 6px;font-size:1.05rem}
  .feature-card p{margin:0;color:var(--muted)}

  /* footer */
  .footer{background:#0f0b1a;color:#ddd;padding:18px 0;margin-top:28px;text-align:center}

  /* modal */
  .modal{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.5);z-index:120;visibility:hidden;opacity:0;transition:opacity .18s}
  .modal.show{visibility:visible;opacity:1}
  .modal-content{background:#fff;padding:18px;border-radius:10px;min-width:320px;max-width:420px;position:relative}
  .modal-close{position:absolute;right:8px;top:8px;background:transparent;border:none;font-size:20px;cursor:pointer}
  .btn-auth{display:block;width:100%;padding:10px;border-radius:8px;border:1px solid #ddd;background:#fff;margin-bottom:8px;cursor:pointer}
  .btn-auth.google{display:flex;gap:10px;align-items:center;justify-content:center}
  .divider{display:flex;align-items:center;gap:8px;margin:8px 0;color:#666}
  .divider span{background:#eee;padding:6px 10px;border-radius:999px}
  .muted{color:var(--muted)}
  .small{font-size:13px;color:#666}

  /* responsive */
  @media (max-width:980px){ .hero-image{display:none} .main-nav{display:none} .search-card{width:100%} }
  @media (max-width:480px){ .hero-title{font-size:1.6rem} .search-controls{flex-direction:column;align-items:stretch} .search-controls select,.search-controls input{width:100%} }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <header class="navbar">
    <div class="navbar-inner container">
      <div class="brand">
        <div class="logo" aria-hidden="true">
          <!-- simple inline house icon as logo -->
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" style="display:block">
            <path d="M3 11.5L12 4l9 7.5V20a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1V11.5z" fill="#ff6b6b"/>
          </svg>
        </div>
        <span class="brand-text">BariBaba</span>
      </div>

      <nav class="main-nav" aria-label="Main Navigation">
        <a href="#" class="nav-link">Buy</a>
        <a href="#" class="nav-link">Rent</a>
        <a href="#" class="nav-link">Commercial</a>
        <a href="#" class="nav-link">PG/Co-Living</a>
        <a href="#" class="nav-link">Plots</a>
      </nav>

      <div class="nav-actions">
        <button id="btnSaved" class="btn-outline">Saved</button>
        <button id="btnLogin" class="btn-post">Sign In</button>
      </div>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero" role="region" aria-label="Hero">
    <div class="hero-overlay"></div>

    <!-- hero image from unsplash, free to use; change if you like -->
    <img src="https://images.unsplash.com/photo-1467269204594-9661b134dd2b?auto=format&fit=crop&w=1600&q=80" alt="" class="hero-image" aria-hidden="true">

    <div class="container hero-content">
      <h1 class="hero-title">Properties to buy in <span id="city-name">Kolkata</span></h1>
      <p class="hero-sub">8K+ listings added daily and 65K+ total verified</p>

      <!-- Search card -->
      <div class="search-card" role="region" aria-label="Property search">
        <ul class="tabs" role="tablist" aria-label="property type tabs">
          <li class="tab active" role="tab" data-type="Buy">BUY</li>
          <li class="tab" role="tab" data-type="Rent">RENT</li>
          <li class="tab" role="tab" data-type="Commercial">COMMERCIAL</li>
          <li class="tab" role="tab" data-type="PG">PG/CO-LIVING</li>
          <li class="tab" role="tab" data-type="Plots">PLOTS</li>
        </ul>

        <div class="search-controls">
          <select id="citySelect" aria-label="City">
            <option>Kolkata</option>
            <option>Delhi</option>
            <option>Mumbai</option>
          </select>

          <input id="searchInput" type="search" placeholder="Search for locality, landmark, project, or builder" aria-label="Search">

          <button id="searchBtn" class="btn-search">Search</button>
        </div>
      </div>

      <!-- Popular localities -->
      <div class="popular" aria-hidden="false">
        <span class="popular-label">Popular Localities</span>
        <div class="chips" id="popularChips" role="list">
          <button class="chip">New Town</button>
          <button class="chip">Rajarhat</button>
          <button class="chip">Behala</button>
          <button class="chip">Madhyamgram</button>
          <button class="chip">Salt Lake</button>
        </div>
      </div>
    </div>
  </section>

  <!-- RESULTS -->
  <main class="container results-section" id="resultsSection">
    <h2 id="resultsTitle">Top listings</h2>
    <div id="resultsGrid" class="results-grid" aria-live="polite"></div>
  </main>

  <!-- FEATURES / HOUSING EDGE -->
  <section class="features container">
    <h2>Housing Edge</h2>
    <p class="muted">Explore property related services</p>

    <div class="feature-grid">
      <div class="feature-card">
        <div class="icon-wrap" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none"><path d="M3 11.5L12 4l9 7.5V20a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1V11.5z" fill="#6B46C1"/></svg>
        </div>
        <div>
          <h3>Buy & Sell</h3>
          <p>Find verified homes and plots with transparent pricing.</p>
        </div>
      </div>

      <div class="feature-card">
        <div class="icon-wrap" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none"><path d="M6 2h12v4H6zM4 8h16v12H4z" fill="#6B46C1"/></svg>
        </div>
        <div>
          <h3>Trusted Agents</h3>
          <p>Connect with verified agents and property managers.</p>
        </div>
      </div>

      <div class="feature-card">
        <div class="icon-wrap" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" fill="#6B46C1"/></svg>
        </div>
        <div>
          <h3>Verified Listings</h3>
          <p>Each listing is checked for authenticity and details.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="container">
      <p>© 2025 BariBaba | help@baribaba.com</p>
    </div>
  </footer>

  <!-- AUTH MODAL (SIMULATED: Google & Phone simulated locally) -->
  <div id="authModal" class="modal" aria-hidden="true">
    <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="authTitle">
      <button class="modal-close" id="closeAuth" aria-label="Close">&times;</button>
      <h3 id="authTitle">Sign in to BariBaba</h3>

      <div style="margin-top:8px;">
        <button id="googleSignIn" class="btn-auth google" title="Simulated Google sign-in">
          <svg width="18" height="18" viewBox="0 0 24 24" style="margin-right:8px" aria-hidden="true"><path fill="#4285F4" d="M21.6 11.2H12v2.8h5.8c-.3 2-2 5.1-5.8 5.1-3.5 0-6.5-2.9-6.5-6.5S8.5 6 12 6c1.9 0 3.3.7 4.1 1.4l1.9-1.9C16.8 4 14.6 3 12 3 6.5 3 2 7.5 2 13s4.5 10 10 10c5 0 9-3.6 9-8.6 0-.8-.1-1.4-.4-3.2z"/></svg>
          Sign in with Google
        </button>
      </div>

      <div class="divider"><span>or</span></div>

      <div class="phone-auth">
        <label style="display:block;margin-bottom:6px">Phone number (e.g. +91 98765 43210)</label>
        <input id="phoneNumber" placeholder="+91 98765 43210" style="width:100%;padding:10px;border-radius:8px;border:1px solid #ddd;margin-bottom:8px">
        <button id="sendOtp" class="btn-auth" style="margin-bottom:6px">Send OTP (simulated)</button>

        <div id="otpRow" style="display:none">
          <input id="otpInput" placeholder="Enter OTP" style="width:100%;padding:10px;border-radius:8px;border:1px solid #ddd;margin-bottom:8px">
          <button id="verifyOtp" class="btn-auth">Verify OTP (simulated)</button>
        </div>
      </div>

      <p class="muted small" style="margin-top:10px">This demo simulates sign-in locally — no external account is required.</p>
      <div id="authStatus" class="muted" style="margin-top:10px"></div>
    </div>
  </div>

  <!-- ---------- JavaScript (all-in-one) ---------- -->
  <script>
  (function(){
    // ---------- Data (sample properties) ----------
    const sampleProperties = [
      { id:1, title:"2 BHK Flat in New Town", price:4500000, city:"Kolkata", location:"New Town", type:"Buy", img:"https://images.unsplash.com/photo-1560184897-6d5483b1e9d0?auto=format&fit=crop&w=800&q=60" },
      { id:2, title:"3 BHK Flat in Rajarhat", price:6000000, city:"Kolkata", location:"Rajarhat", type:"Buy", img:"https://images.unsplash.com/photo-1572120360610-d971b9b7b7b6?auto=format&fit=crop&w=800&q=60" },
      { id:3, title:"Furnished PG in Behala", price:5500, city:"Kolkata", location:"Behala", type:"PG", img:"https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=800&q=60" },
      { id:4, title:"Plot 500 sq yd in Dwarka", price:12000000, city:"Delhi", location:"Dwarka", type:"Plots", img:"https://images.unsplash.com/photo-1505842465776-3d1a0b1f4b8b?auto=format&fit=crop&w=800&q=60" },
      { id:5, title:"1 BHK Furnished, Salt Lake", price:12000, city:"Kolkata", location:"Salt Lake", type:"Rent", img:"https://images.unsplash.com/photo-1560448204-e02f11c3d0e2?auto=format&fit=crop&w=800&q=60" }
    ];

    // ---------- Elements ----------
    const tabs = document.querySelectorAll(".tab");
    const citySelect = document.getElementById("citySelect");
    const cityName = document.getElementById("city-name");
    const searchInput = document.getElementById("searchInput");
    const searchBtn = document.getElementById("searchBtn");
    const chips = document.querySelectorAll(".chip");
    const resultsGrid = document.getElementById("resultsGrid");
    const resultsTitle = document.getElementById("resultsTitle");
    const btnLogin = document.getElementById("btnLogin");
    const authModal = document.getElementById("authModal");
    const closeAuth = document.getElementById("closeAuth");
    const googleSignIn = document.getElementById("googleSignIn");
    const sendOtp = document.getElementById("sendOtp");
    const verifyOtp = document.getElementById("verifyOtp");
    const otpRow = document.getElementById("otpRow");
    const phoneNumberInput = document.getElementById("phoneNumber");
    const otpInput = document.getElementById("otpInput");
    const authStatus = document.getElementById("authStatus");

    // ---------- Utilities ----------
    function escapeHtml(s){ return (s||'').toString().replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
    function showModal(){ authModal.classList.add("show"); authModal.setAttribute("aria-hidden","false"); }
    function hideModal(){ authModal.classList.remove("show"); authModal.setAttribute("aria-hidden","true"); }

    // ---------- Tabs behavior ----------
    tabs.forEach(t => t.addEventListener("click", () => {
      tabs.forEach(x => x.classList.remove("active"));
      t.classList.add("active");
    }));

    // ---------- City select updates displayed city ----------
    citySelect.addEventListener("change", () => {
      cityName.textContent = citySelect.value;
    });

    // ---------- Render results ----------
    function renderResults(list, label){
      resultsGrid.innerHTML = "";
      if(!list || list.length === 0){
        resultsGrid.innerHTML = "<p>No properties found.</p>";
        resultsTitle.textContent = label ? `No results for "${label}"` : "No results";
        return;
      }
      resultsTitle.textContent = label || "Search results";
      list.forEach(p => {
        const el = document.createElement("div");
        el.className = "card";
        el.innerHTML = `
          <img src="${p.img}" alt="${escapeHtml(p.title)}">
          <div class="card-body">
            <h3>${escapeHtml(p.title)}</h3>
            <p class="muted">${escapeHtml(p.location)}, ${escapeHtml(p.city)}</p>
            <p class="price">₹${Number(p.price).toLocaleString()}</p>
          </div>
        `;
        resultsGrid.appendChild(el);
      });
    }

    // ---------- Search logic ----------
    function doSearch(){
      const activeTab = document.querySelector(".tab.active");
      const type = activeTab ? activeTab.dataset.type : null;
      const city = citySelect.value;
      const q = searchInput.value.trim().toLowerCase();

      let filtered = sampleProperties.filter(p => {
        let ok = true;
        if (type) ok = ok && p.type.toLowerCase() === type.toLowerCase();
        if (city) ok = ok && p.city.toLowerCase() === city.toLowerCase();
        if (q) ok = ok && (p.location.toLowerCase().includes(q) || p.title.toLowerCase().includes(q));
        return ok;
      });

      // friendly label
      const label = q ? q : `${type} in ${city}`;
      renderResults(filtered, label);
      // scroll to results (good UX)
      document.getElementById("resultsSection").scrollIntoView({behavior:"smooth", block:"start"});
    }

    // hook search
    searchBtn.addEventListener("click", doSearch);
    searchInput.addEventListener("keydown", (e) => { if (e.key === "Enter") doSearch(); });

    // chips quick search
    chips.forEach(c => c.addEventListener("click", () => {
      searchInput.value = c.textContent;
      doSearch();
    }));

    // initial render
    renderResults(sampleProperties.filter(p => p.type === "Buy"), "Top listings");

    // ---------- AUTH (SIMULATED) ----------
    function setUser(user){
      localStorage.setItem("baribaba_user", JSON.stringify(user));
      btnLogin.textContent = user.name || user.phone || "Profile";
    }
    function clearUser(){
      localStorage.removeItem("baribaba_user");
      btnLogin.textContent = "Sign In";
    }
    function getUser(){ try{ return JSON.parse(localStorage.getItem("baribaba_user")); }catch(e){return null;} }

    // show modal
    btnLogin.addEventListener("click", () => {
      if(getUser()){ // already signed in -> show profile quick action
        if(confirm("You are signed in. Do you want to sign out?")){ clearUser(); alert("Signed out."); }
      } else {
        showModal();
      }
    });
    closeAuth.addEventListener("click", hideModal);

    // Simulated Google sign-in
    googleSignIn.addEventListener("click", () => {
      // prompt for name (simulation)
      const name = prompt("Simulated Google sign-in\nEnter your name to simulate sign-in:");
      if(name){
        const user = { provider:"google", name: name, email: (name.replace(/\s+/g,'.').toLowerCase()+'@example.com') };
        setUser(user);
        authStatus.textContent = `Signed in as ${user.name} (simulated)`;
        hideModal();
      } else {
        authStatus.textContent = "Google sign-in cancelled.";
      }
    });

    // Simulated phone OTP flow
    sendOtp.addEventListener("click", () => {
      const phone = phoneNumberInput.value.trim();
      if(!phone){ authStatus.textContent = "Enter a phone number with country code."; return; }
      // "send" OTP (simulate)
      authStatus.textContent = "OTP sent (simulated). Use code 123456.";
      otpRow.style.display = "block";
      sessionStorage.setItem("sim_otp", "123456");
    });

    verifyOtp.addEventListener("click", () => {
      const code = otpInput.value.trim();
      const expected = sessionStorage.getItem("sim_otp");
      if(code === expected){
        const user = { provider:"phone", phone: phoneNumberInput.value.trim() };
        setUser(user);
        authStatus.textContent = `Signed in as ${user.phone} (simulated)`;
        hideModal();
      } else {
        authStatus.textContent = "Invalid OTP (simulated). Try 123456.";
      }
    });

    // restore user if present
    const restored = getUser();
    if(restored){ btnLogin.textContent = restored.name || restored.phone || "Profile"; }

    // close modal on outside click
    authModal.addEventListener("click", (e) => { if(e.target === authModal) hideModal(); });

  })();
  </script>

</body>
</html>
